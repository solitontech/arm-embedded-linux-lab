# New Project Scaffold

A minimal but correctly-wired starting point for any new project in this monorepo.

## Usage
Copy this entire directory to `projects/<your-project-name>/` and replace placeholder values.

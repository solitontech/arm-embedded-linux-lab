# ==============================================================================
# Project Makefile Template
# Copy to projects/<your-project>/ and customize variables below.
# ==============================================================================

# Project identity
PROJECT_NAME    := mytest
PROJECT_TYPE    := app            # app | lib_static | lib_shared | raw
DEFAULT_BOARD   := host

# Custom build flags (optional)
# CXXFLAGS_EXTRA  += -std=c++20
# CFLAGS_EXTRA    += 
# INCLUDES_EXTRA  += -I./include
# LDLIBS_EXTRA    += -lpthread

# Deployment configuration (optional overrides)
# DEPLOY_METHOD   := ssh          # ssh | tftp | nfs | custom
# TARGET_DEST_DIR := /usr/local/bin

# Lifecycle hooks (optional)
# PRE_BUILD_HOOK   := 
# POST_BUILD_HOOK  := 
# PRE_DEPLOY_HOOK  := 
# POST_DEPLOY_HOOK := 

# Include the centralized monorepo framework
include ../../shared/build_system/framework.mk
